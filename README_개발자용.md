# 🚀 지자체 합동평가 시뮬레이터 - 개발자 가이드

**버전:** 2.0.0 (오프라인)  
**최종 업데이트:** 2025년 10월 2일

---

## 📋 빠른 시작

### 개발 서버 실행
```bash
npm install     # 의존성 설치
npm run dev     # 개발 서버 (http://localhost:5173)
```

### 배포 패키지 생성
```bash
npm run package  # 빌드 + 오프라인 배포 패키지 생성
```

### 테스트
```bash
테스트_실행.bat   # 배포 패키지 테스트
npm start        # 로컬 서버만 실행
```

---

## 🔄 온라인 버전에서 업데이트하기

### 자동 방식 (추천)
```bash
1. 온라인버전_to_오프라인버전_업데이트.bat 실행
2. 온라인 버전 경로 입력
3. CDN 확인 (public/calculation.worker.js, index.html)
4. npm run package
5. 테스트_실행.bat
```

### 수동 방식
```bash
1. 온라인 버전 src/ 복사
2. 필요한 public/ 파일 복사
3. CDN → 로컬 경로 변경
   - calculation.worker.js: importScripts('/xlsx.full.min.js')
   - index.html: Font Awesome CDN 제거
4. npm run package
```

**자세한 내용:** `업데이트_워크플로우_가이드.md` 참조

---

## 📦 프로젝트 구조

```
프로젝트/
├── src/                           # React 소스 코드
├── public/                        # 정적 파일
│   ├── xlsx.full.min.js          ⭐ 오프라인: XLSX 로컬
│   └── calculation.worker.js     ⭐ 오프라인: Worker
├── dist/                          # 빌드 결과물 (자동 생성)
├── 오프라인_배포_패키지/           # 배포용 (자동 생성)
│
├── server.cjs                     # 로컬 웹 서버
├── create-offline-package.cjs     # 패키징 스크립트
├── 시뮬레이터_실행.bat             # 실행 파일
│
└── 문서들/
    ├── 오프라인_배포_가이드.md     # 사용자용 배포 가이드
    ├── 업데이트_워크플로우_가이드.md  # 온라인→오프라인 업데이트
    ├── USB_배포_체크리스트.md      # 배포 체크리스트
    └── 프로젝트_파일_정리_가이드.md  # 파일 구조 설명
```

---

## 🎯 핵심 차이점: 온라인 vs 오프라인

| 구분 | 온라인 버전 | 오프라인 버전 |
|------|-----------|-------------|
| Font Awesome | CDN | npm 패키지 (`@fortawesome/fontawesome-free`) |
| XLSX | CDN | 로컬 파일 (`public/xlsx.full.min.js`) |
| 배포 | Netlify/Vercel | USB/내부망 |
| 실행 | URL 접속 | `시뮬레이터_실행.bat` |
| 서버 | 클라우드 | Node.js 로컬 (`server.cjs`) |

---

## 🔍 CDN 체크 방법

### 자동 검색
```bash
# PowerShell
Select-String -Path src\*,public\*,index.html -Pattern "https://" -Exclude "*.md"

# Git Bash / Linux
grep -r "https://" src/ public/ index.html | grep -v xmlns
```

### 확인 포인트
1. **index.html** - CDN 링크 없어야 함
2. **src/main.jsx** - `import '@fortawesome/fontawesome-free/css/all.min.css'`
3. **public/calculation.worker.js** - `self.importScripts('/xlsx.full.min.js')`
4. **public/xlsx.full.min.js** - 파일 존재 확인

---

## 📝 개발 워크플로우

### 기능 개발 (온라인 버전)
```bash
1. 온라인 버전 폴더에서 작업
2. 기능 개발 및 테스트
3. Git commit & push
4. Netlify 자동 배포
```

### 오프라인 버전 업데이트
```bash
1. 온라인버전_to_오프라인버전_업데이트.bat
2. CDN 확인 및 수정
3. npm run package
4. 테스트_실행.bat
5. USB 배포
```

---

## 🛠️ NPM Scripts

```json
{
  "dev": "vite",                    // 개발 서버
  "build": "vite build",            // 프로덕션 빌드
  "preview": "vite preview",        // 빌드 미리보기
  "start": "node server.cjs",       // 로컬 서버만 실행
  "package": "npm run build && node create-offline-package.cjs"  // 빌드+패키징
}
```

---

## 🐛 트러블슈팅

### Q1. "NetworkError: Failed to load xlsx"
**A:** 
```bash
# xlsx 파일 확인
ls public/xlsx.full.min.js

# 없으면 복사
xcopy /Y node_modules\xlsx\dist\xlsx.full.min.js public\

# 다시 빌드
npm run build
```

### Q2. 아이콘 깨짐 (□□□)
**A:**
```bash
# Font Awesome 설치 확인
npm list @fortawesome/fontawesome-free

# 없으면 설치
npm install @fortawesome/fontawesome-free

# main.jsx 확인
# import '@fortawesome/fontawesome-free/css/all.min.css' 있어야 함

# 다시 빌드
npm run build
```

### Q3. "require is not defined" 오류
**A:**
```bash
# CommonJS 파일은 .cjs 확장자 사용
server.cjs  ✅
create-offline-package.cjs  ✅
```

### Q4. 빌드는 되는데 오프라인에서 안 됨
**A:**
```bash
# CDN 사용 여부 확인
grep -r "cdn\|https://" src/ public/ index.html

# dist/ 안에 필요한 파일 확인
ls dist/assets/fa-*.woff2      # 3개 있어야 함
ls dist/xlsx.full.min.js       # 있어야 함
```

---

## 📚 문서 가이드

| 문서 | 대상 | 내용 |
|------|------|------|
| `README_개발자용.md` (현재) | 개발자 | 개발/빌드/배포 전체 |
| `오프라인_배포_가이드.md` | 사용자 | 설치 및 사용법 |
| `업데이트_워크플로우_가이드.md` | 개발자 | 온라인→오프라인 업데이트 |
| `USB_배포_체크리스트.md` | 개발자 | 배포 체크리스트 |
| `프로젝트_파일_정리_가이드.md` | 개발자 | 파일 구조 설명 |
| `오프라인_버전_완성_요약.md` | 개발자 | 완성 보고서 |

---

## 🔐 환경 변수

### .env.local (로컬 개발)
```env
VITE_MASTER_KEY="k.infra"
```

### .env.production (프로덕션)
```env
VITE_MASTER_KEY="production_key"
```

---

## 📦 배포

### USB 배포
```bash
1. npm run package
2. 오프라인_배포_패키지/ 폴더를 USB에 복사
3. Node.js 설치 파일도 함께 복사
   - https://nodejs.org/
   - node-v20.x.x-x64.msi
```

### 배포 패키지 내용
```
오프라인_배포_패키지/
├── dist/                          # 빌드된 앱
├── server.cjs                     # 로컬 서버
├── 시뮬레이터_실행.bat             # 실행 파일
├── 오프라인_배포_가이드.md         # 사용자 가이드
└── README_배포용.txt              # 빠른 시작
```

---

## 🎯 체크리스트

### 빌드 전
- [ ] 모든 CDN 제거 확인
- [ ] `public/xlsx.full.min.js` 존재
- [ ] `@fortawesome/fontawesome-free` 설치됨
- [ ] 의존성 최신화 (`npm install`)

### 빌드
- [ ] `npm run build` 성공
- [ ] `dist/assets/` 안에 `fa-*.woff2` 3개
- [ ] `dist/xlsx.full.min.js` 존재
- [ ] 경고 메시지 확인 (청크 크기는 무시 가능)

### 패키징
- [ ] `npm run package` 성공
- [ ] `오프라인_배포_패키지/` 생성 확인
- [ ] 모든 필요 파일 포함 확인

### 테스트
- [ ] `테스트_실행.bat` 실행
- [ ] 브라우저 자동 오픈
- [ ] 모든 기능 작동
- [ ] 아이콘 정상 표시
- [ ] Excel 업로드 작동
- [ ] 계산 결과 정상

### 배포
- [ ] Node.js 설치 파일 준비
- [ ] USB에 복사
- [ ] 사용자 PC에서 테스트
- [ ] 문서 포함 확인

---

## 📞 문의

**국토안전관리원 기반시설관리실**
- 담당자: 김민웅
- 연락처: 055-771-8497

---

**제작:** 김민웅 ⓒ 2025 All rights reserved.





