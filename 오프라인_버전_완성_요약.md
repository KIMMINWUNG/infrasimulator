# 🎉 지자체 합동평가 시뮬레이터 - 오프라인 버전 완성!

**작성일:** 2025년 10월 2일  
**버전:** 2.0.0 (완전 오프라인)

---

## ✅ 완성 사항

### 1. **외부 의존성 제거 (100% 오프라인)**

| 항목 | 이전 (온라인 필요) | 이후 (오프라인) |
|------|-------------------|----------------|
| Font Awesome | CDN | npm 패키지 (로컬) |
| XLSX 라이브러리 | CDN | 로컬 파일 |
| 기타 리소스 | - | 모두 로컬 포함 |

### 2. **배포 시스템 구축**

✅ **간편한 실행 파일**
- `시뮬레이터_실행.bat` - 더블클릭만 하면 실행

✅ **자동 배포 패키지 생성**
- `npm run package` - 한 번에 빌드 + 패키징

✅ **상세한 가이드 문서**
- `설치_가이드.txt` - 간단 설명
- `오프라인_배포_가이드.md` - 상세 매뉴얼
- `USB_배포_체크리스트.md` - 배포 체크리스트

---

## 📁 파일 구조

```
프로젝트/
├── 오프라인_배포_패키지/              ← USB에 복사할 폴더
│   ├── dist/                         ← 빌드된 앱
│   │   ├── assets/
│   │   │   ├── fa-brands-400.woff2  (101KB)
│   │   │   ├── fa-regular-400.woff2 (19KB)
│   │   │   ├── fa-solid-900.woff2   (113KB)
│   │   │   ├── index.css            (100KB)
│   │   │   └── index.js             (638KB)
│   │   ├── xlsx.full.min.js         (로컬 XLSX)
│   │   ├── calculation.worker.js
│   │   ├── index.html
│   │   ├── ci_logo.png
│   │   └── management_list.xlsx
│   ├── server.cjs                    ← 로컬 서버
│   ├── 시뮬레이터_실행.bat            ← 실행 파일
│   ├── 설치_가이드.txt
│   ├── 오프라인_배포_가이드.md
│   └── README_배포용.txt
│
├── src/                              ← 개발 소스 (배포 불필요)
├── public/                           ← 정적 파일 (배포 불필요)
├── node_modules/                     ← 개발 의존성 (배포 불필요)
│
├── 테스트_실행.bat                    ← 로컬 테스트용
├── USB_배포_체크리스트.md             ← 배포 가이드
└── package.json
```

---

## 🔧 개발자 명령어

### 개발 시
```bash
npm run dev          # 개발 서버 실행
npm run build        # 프로덕션 빌드
```

### 배포 준비
```bash
npm run package      # 빌드 + 배포 패키지 자동 생성
```

### 테스트
```bash
npm start            # 현재 폴더에서 서버 실행
테스트_실행.bat       # 배포 패키지 테스트
```

---

## 📦 USB 배포 방법

### 1단계: Node.js 다운로드 (인터넷 되는 곳에서)
```
https://nodejs.org/
→ "LTS" 버전 다운로드
→ Windows Installer (x64) 선택
→ 파일명: node-v20.x.x-x64.msi (약 30MB)
```

### 2단계: USB에 복사
```
USB/
├── node-v20.x.x-x64.msi              ← Node.js 설치 파일
└── 시뮬레이터/                        ← 오프라인_배포_패키지 폴더 복사
    └── (위 파일들...)
```

### 3단계: 사용자 PC (내부망, 인터넷 없음)
```
1. node-v20.x.x-x64.msi 설치 (1분)
2. 시뮬레이터 폴더 원하는 곳에 복사
3. 시뮬레이터_실행.bat 더블클릭
4. 끝! ✅
```

---

## 🎯 주요 기능

### ✅ 완전 오프라인 작동
- 인터넷 연결 전혀 불필요
- 모든 리소스 로컬 포함
- 외부 CDN 의존성 0

### ✅ 간편한 실행
- 더블클릭으로 실행
- 자동 브라우저 오픈
- 별도 설치 불필요

### ✅ 이식성
- USB/네트워크 드라이브로 배포
- 폴더 복사만으로 설치
- 여러 PC에서 독립 실행

### ✅ 안정성
- Node.js 기반 로컬 서버
- 에러 처리 완비
- 한글 완벽 지원

---

## 🐛 문제 해결

### Q1. "Node.js가 설치되어 있지 않습니다"
**A:** Node.js 설치 후 컴퓨터 재시작

### Q2. 브라우저가 자동으로 안 열림
**A:** 브라우저에서 `http://localhost:3000` 직접 입력

### Q3. 아이콘이 깨져 보임 (□□□)
**A:** 
- Font Awesome 파일 확인: `dist/assets/fa-*.woff2`
- 파일이 있으면: 브라우저 캐시 삭제 후 새로고침
- 파일이 없으면: 배포 패키지 재생성

### Q4. "Failed to load xlsx.full.min.js" 오류
**A:**
- `dist/xlsx.full.min.js` 파일 확인
- 없으면 다시 빌드: `npm run package`

---

## 📊 성능 지표

| 항목 | 수치 |
|------|------|
| 전체 용량 | ~2MB |
| 빌드 시간 | ~3초 |
| 로딩 시간 | <1초 (로컬) |
| Font Awesome | 233KB (3개 파일) |
| XLSX 라이브러리 | 포함됨 |
| 브라우저 호환성 | Chrome, Edge, Firefox |

---

## 🔐 보안

### 완전 로컬 실행
- `localhost`에서만 접근 가능
- 외부 네트워크 노출 없음
- 데이터 서버 전송 없음

### 인증 시스템
- 기본 KEY: `k.infra`
- 변경 필요 시 `.env` 파일 수정

---

## 📝 버전 히스토리

### v2.0.0 (2025-10-02) - 오프라인 버전
- ✅ Font Awesome 로컬화
- ✅ XLSX 라이브러리 로컬화
- ✅ 배포 시스템 구축
- ✅ 완전 오프라인 지원

### v1.x - 온라인 버전
- 기본 기능 구현
- CDN 사용 (인터넷 필요)

---

## 📞 문의

**국토안전관리원 기반시설관리실**
- 담당자: 김민웅
- 연락처: 055-771-8497
- 주소: 경상남도 진주시 사들로 123번길 40, 7층

---

## 📚 참고 문서

1. `설치_가이드.txt` - 빠른 시작 가이드
2. `오프라인_배포_가이드.md` - 상세 배포 매뉴얼
3. `USB_배포_체크리스트.md` - 배포 체크리스트
4. `README.md` - 프로젝트 개요

---

**제작:** 김민웅 ⓒ 2025 All rights reserved.





