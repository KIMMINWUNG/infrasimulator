# 🔄 온라인 버전 → 오프라인 버전 업데이트 가이드

## 📋 워크플로우 개요

```
인터넷 버전 (기능 개발)
    ↓ 복사 및 변환
오프라인 버전 (내부망 배포)
```

---

## 🎯 추천 방식: 두 프로젝트 분리 관리

### **폴더 구조**
```
개발/
├── 시뮬레이터_온라인/          ← 인터넷 버전 (기능 개발용)
│   ├── src/
│   ├── public/
│   └── ... (CDN 사용, Netlify 배포)
│
└── 시뮬레이터_오프라인/         ← 오프라인 버전 (내부망 배포용)
    ├── src/
    ├── public/
    ├── 오프라인_배포_패키지/
    └── ... (로컬 파일 사용, USB 배포)
```

---

## 🔄 업데이트 프로세스

### **방법 1: 자동 스크립트 (추천)** ⭐

#### 1단계: 스크립트 실행
```bash
온라인버전_to_오프라인버전_업데이트.bat
```

#### 2단계: 경로 입력
```
온라인 버전 프로젝트 경로: D:\프로젝트\시뮬레이터_온라인
```

#### 3단계: 자동 복사
- ✅ `src/` 폴더 전체
- ✅ `public/` 필요한 파일들
- ✅ `index.html`

#### 4단계: 수동 확인 (중요!)
```
1. public/calculation.worker.js 확인
   - CDN → 로컬 경로 변경

2. index.html 확인
   - Font Awesome CDN 제거 확인

3. 전체 CDN 검색
   - "https://" 또는 "cdn" 검색
```

#### 5단계: 빌드 및 테스트
```bash
npm run package      # 빌드 + 배포 패키지 생성
테스트_실행.bat       # 테스트
```

---

### **방법 2: 수동 복사**

#### 복사할 것
```
온라인 버전에서 복사:
✅ src/ 폴더 전체
✅ public/ci_logo.png
✅ public/management_list.xlsx
✅ public/calculation.worker.js (수정 필요)
✅ index.html
✅ package.json 의존성 확인
```

#### 복사하지 말 것
```
❌ node_modules/
❌ dist/
❌ .env (다를 수 있음)
❌ netlify.toml (온라인 전용)
❌ .git/ (버전 관리 분리)
```

#### 수정할 것
```
1. public/calculation.worker.js
   변경 전: self.importScripts('https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js');
   변경 후: self.importScripts('/xlsx.full.min.js');

2. index.html
   제거: <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/...">

3. src/main.jsx
   확인: import '@fortawesome/fontawesome-free/css/all.min.css' 있는지
```

---

## 🔍 CDN 체크리스트

업데이트 후 반드시 확인:

### 1. Font Awesome
```bash
# 검색: "font-awesome" + "cdn"
# 위치: index.html, src/main.jsx

✅ 올바른 상태:
- index.html: CDN 링크 없음
- src/main.jsx: import '@fortawesome/fontawesome-free/css/all.min.css'
```

### 2. XLSX 라이브러리
```bash
# 검색: "xlsx" + "cdn" + "jsdelivr"
# 위치: public/calculation.worker.js

✅ 올바른 상태:
- self.importScripts('/xlsx.full.min.js')
- public/xlsx.full.min.js 파일 존재
```

### 3. 기타 외부 리소스
```bash
# 전체 프로젝트에서 검색
# 검색어: "https://" (SVG xmlns 제외)

✅ 허용되는 것들:
- xmlns="http://..." (SVG, XML 네임스페이스)
- 라이선스 주석 내부 URL
```

---

## 🛠️ 빠른 검증 방법

### 1. CDN 사용 여부 확인
```bash
# 프로젝트 루트에서 실행
grep -r "cdn" src/ public/ index.html
grep -r "https://" src/ public/ index.html
```

### 2. 필수 로컬 파일 확인
```bash
✅ public/xlsx.full.min.js
✅ node_modules/@fortawesome/fontawesome-free/
```

### 3. 빌드 결과 확인
```bash
npm run build
# dist/assets/ 안에 fa-*.woff2 파일 3개 확인
# dist/xlsx.full.min.js 파일 확인
```

---

## 📝 상세 단계별 가이드

### Step 1: 온라인 버전 최신화
```bash
# 온라인 버전 폴더에서
git pull        # 최신 코드 받기 (Git 사용 시)
npm install     # 의존성 업데이트
npm run build   # 테스트 빌드
```

### Step 2: 오프라인 버전으로 복사
```bash
# 오프라인 버전 폴더에서
온라인버전_to_오프라인버전_업데이트.bat 실행
```

### Step 3: CDN → 로컬 변환
```bash
# public/calculation.worker.js 수정
# index.html 확인
# src/main.jsx 확인
```

### Step 4: 의존성 동기화
```bash
# package.json 비교
# 온라인 버전에 새 패키지가 있으면
npm install <새패키지명>
```

### Step 5: 로컬 파일 준비
```bash
# xlsx 파일 최신화 (필요시)
xcopy /Y node_modules\xlsx\dist\xlsx.full.min.js public\
```

### Step 6: 빌드 및 테스트
```bash
npm run package          # 빌드 + 패키징
테스트_실행.bat           # 로컬 테스트
```

### Step 7: 배포 패키지 확인
```bash
# 오프라인_배포_패키지/ 폴더 확인
# - dist/assets/ 안에 폰트 파일
# - dist/xlsx.full.min.js
# - 모든 기능 정상 작동
```

---

## ⚠️ 주의사항

### 1. 새 외부 라이브러리 추가 시
온라인 버전에서 CDN으로 추가한 라이브러리가 있다면:

```bash
# npm 패키지로 설치
npm install <라이브러리명>

# import로 사용
import 'library/dist/library.css'
```

### 2. 환경 변수
```bash
# .env 파일은 별도 관리
온라인 버전: VITE_MASTER_KEY="online_key"
오프라인 버전: VITE_MASTER_KEY="offline_key"
```

### 3. 설정 파일
```bash
# 온라인 전용 (복사 금지)
netlify.toml
vercel.json

# 오프라인 전용 (유지)
server.cjs
시뮬레이터_실행.bat
```

---

## 🎯 자동화 개선 아이디어

### 향후 고도화 방안

#### 1. 통합 프로젝트 관리
```javascript
// vite.config.js
export default defineConfig({
  define: {
    'import.meta.env.OFFLINE_MODE': process.env.OFFLINE_MODE === 'true'
  }
})

// 코드에서
if (import.meta.env.OFFLINE_MODE) {
  // 오프라인 모드 로직
} else {
  // 온라인 모드 로직
}
```

#### 2. Git 브랜치 전략
```bash
main          # 온라인 버전
└── offline   # 오프라인 버전 (main에서 정기적으로 merge)
```

#### 3. 자동 변환 스크립트
```javascript
// convert-to-offline.js
// CDN 자동 탐지 및 로컬 경로 변환
```

---

## 📊 비교표

| 항목 | 온라인 버전 | 오프라인 버전 |
|------|-----------|-------------|
| Font Awesome | CDN | npm 패키지 |
| XLSX | CDN | 로컬 파일 |
| 배포 방식 | Netlify/Vercel | USB/내부망 |
| 실행 방법 | URL 접속 | 배치 파일 |
| 서버 | 클라우드 | Node.js (로컬) |
| 업데이트 | Git push | USB 재배포 |

---

## 🚀 빠른 참조

### 업데이트 명령어
```bash
# 1. 자동 복사
온라인버전_to_오프라인버전_업데이트.bat

# 2. CDN 확인
grep -r "cdn\|https://" src/ public/ index.html

# 3. 빌드
npm run package

# 4. 테스트
테스트_실행.bat
```

### CDN 변환 패턴
```javascript
// 1. Font Awesome
❌ <link href="https://cdn.../font-awesome.css">
✅ import '@fortawesome/fontawesome-free/css/all.min.css'

// 2. XLSX
❌ self.importScripts('https://cdn.../xlsx.min.js')
✅ self.importScripts('/xlsx.full.min.js')

// 3. 새 라이브러리 추가 시
❌ <script src="https://cdn.../library.js">
✅ npm install library → import 'library'
```

---

**작성일:** 2025년 10월 2일  
**버전:** 1.0





